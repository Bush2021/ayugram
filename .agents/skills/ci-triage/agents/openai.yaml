interface:
  display_name: "CI Triage"
  short_description: "Diagnose the latest failing GitHub Actions run"
  default_prompt: "Use $ci-triage to diagnose the latest failing CI run and recommend the smallest fix."
